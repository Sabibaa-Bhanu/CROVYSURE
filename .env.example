# CROVYSURE Environment Configuration
# When deploying to Vercel without a connected backend, leave this empty or unset.
# The application will automatically operate in self-contained Prototype / Demo Mode.

# Optional: Base URL for future FastAPI backend service
VITE_API_BASE_URL=
